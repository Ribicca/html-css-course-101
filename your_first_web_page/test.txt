//Print1To255()

var i;
for (i=1; i<=255; i++) {
    console.log(i);
}
console.log(i);

//PrintOdds1To255()-----Print all odd integers from 1 to 255.       
var i;
for(i=1;i<=255; i++){
	if (i%2 !=0){
		console.log(i);
	}
}

//PrintNumbersAndSum0to255()-Print Numbers from 0 to 255, and with each integer print the sum so far.
var i;
var sum;
for(i=1; i<=255; i++){
 sum++;
}
console.log(sum);

//PrindArrayVals(arr)-Print each value in a given array

 function PrindArrayVals(arr) {
   
for (var i = 0; i < arr.length; i++) {
    if  (arr[i]===0) {
         }
     }
     return arr;
 }


//PrintMaxOfArray(arr)-Print the largest element in a given


//PrintAverageOfArray(arr)-Analyze an array’s values and print the average.


//ReturnOddsArray1To255()-Create and return array with odd numbers from 1  to 255.

//SquareArrayVals(arr)-Given an array, square each value in the array.

//ReturnArrayCountGreaterThanX(arr,X)-Given an array, return the count that is greater than Y

//ZeroOutArrayNegativeVals(arr)-Given an Array, set negative values to zero.


//PrintMaxMinAverageArrayVals(arr)-Given an array, print max, min and average values.


//ShiftArrayValsLeft(arr)-Given an array, shift values forward by one, dropping the first value and leaving an extra “0” value at the end


//SwappStringForArrayNegativeVals(arr)-Given an array, replace negative values with ‘fga’
